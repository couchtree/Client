namespace DataContainer
{
    /// <summary>
    /// Struct to store all garden related data.
    /// It acts as a container for save and load operations w/o the need to instantiate the whole MyGarden class
    /// </summary>
    public struct GardenData
    {
        public string name; // name of the garden
    }
}